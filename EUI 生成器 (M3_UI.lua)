local p = game.Players.LocalPlayer
local UIS = game:GetService("UserInputService")
local function createUI()
    print("//// #### [UI] GENERATING_CONSOLE_INTERFACE #### ////")
    if p.PlayerGui:FindFirstChild("E5_Console") then p.PlayerGui.E5_Console:Destroy() end
    -- [此處貼入你原始碼 createUI 的所有 UI 佈局代碼]
    -- 注意：將所有 fKN 改為 _G.fKN, 所有 freezeMS 改為 _G.freezeMS
    -- 範例：l.Text = _G.fKN
    -- 範例：_G.freezeMS = math.clamp(val, 0, 5000)
end
createUI()
p.CharacterAdded:Connect(function() task.wait(0.5) createUI() end)
